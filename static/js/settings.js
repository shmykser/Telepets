// ========================================
// НАСТРОЙКИ ТЕСТИРОВАНИЯ
// ========================================

// ========================================
// НАСТРОЙКИ СВАЙПОВ И ВЗАИМОДЕЙСТВИЯ
// ========================================

// Размер свайпа (в процентах от ширины яйца)
const SWIPE_THRESHOLD_PERCENT = 50; // 50% = 100px при ширине яйца 200px

// Количество свайпов для нагрева на 1 градус
const SWIPES_PER_DEGREE = 50;

// Время сброса счетчика свайпов (в миллисекундах)
const SWIPE_RESET_TIME = 3000; // 3 секунды

// Скорость уменьшения прогресса свайпов (в миллисекундах)
const PROGRESS_DECAY_TIME = 400; // 0.4 секунды

// ========================================
// НАСТРОЙКИ ТЕМПЕРАТУРЫ
// ========================================

// Критически низкая температура (таймер останавливается)
const CRITICAL_LOW_TEMP = 28;

// Критически высокая температура (таймер останавливается)
const CRITICAL_HIGH_TEMP = 41;

// Смертельная низкая температура
const DEAD_LOW_TEMP = 18;

// Смертельная высокая температура
const DEAD_HIGH_TEMP = 44;

// Нормальная температура для инкубации
const NORMAL_TEMP_MIN = 28;
const NORMAL_TEMP_MAX = 41;

// ========================================
// НАСТРОЙКИ ВРЕМЕНИ
// ========================================

// Время инкубации (в часах)
const INCUBATION_TIME_HOURS = 72; // 3 дня

// Время остывания яйца (в секундах)
const COOLING_INTERVAL_SECONDS = 60; // 1 минута

// Интервал обновления данных с сервера (в миллисекундах)
const DATA_UPDATE_INTERVAL = 10000; // 10 секунд

// Интервал обновления таймера (в миллисекундах)
const TIMER_UPDATE_INTERVAL = 1000; // 1 секунда

// ========================================
// НАСТРОЙКИ РАЗМЕРОВ И ВИЗУАЛИЗАЦИИ
// ========================================

// Ширина яйца в пикселях
const EGG_WIDTH = 200;

// Высота яйца в пикселях
const EGG_HEIGHT = 250;

// Размер шкалы прогресса свайпов
const PROGRESS_BAR_WIDTH = 200;
const PROGRESS_BAR_HEIGHT = 8;

// ========================================
// НАСТРОЙКИ АНИМАЦИЙ
// ========================================

// Длительность анимации нагрева (в миллисекундах)
const WARMING_ANIMATION_DURATION = 500;

// Длительность анимации трения (в миллисекундах)
const RUBBING_ANIMATION_DURATION = 300;

// Длительность анимации +1°C (в миллисекундах)
const TEMP_INCREASE_ANIMATION_DURATION = 1000;

// ========================================
// НАСТРОЙКИ ТЕСТИРОВАНИЯ
// ========================================

// Тестовый User ID (используется если не получен из Telegram)
const TEST_USER_ID = 273065571;

// Режим отладки (показывает дополнительную информацию в консоли)
const DEBUG_MODE = true;

// Быстрый режим для тестирования (уменьшает время инкубации)
const FAST_MODE = false;
const FAST_MODE_MULTIPLIER = 0.1; // 10% от обычного времени

// ========================================
// НАСТРОЙКИ СООБЩЕНИЙ
// ========================================

// Сообщения для различных состояний
const MESSAGES = {
    CRITICAL_LOW: "⚠️ КРИТИЧЕСКИ НИЗКАЯ ТЕМПЕРАТУРА! Трите яйцо!",
    CRITICAL_HIGH: "⚠️ КРИТИЧЕСКИ ВЫСОКАЯ ТЕМПЕРАТУРА!",
    LOW_TEMP: "🌡️ Температура низкая. Трите яйцо для нагрева.",
    DEAD: "💀 Яйцо погибло от экстремальной температуры!",
    EGG_NOT_FOUND: "Яйцо не найдено",
    LOADING_ERROR: "Ошибка загрузки",
    OVERHEAT_DEATH: "Яйцо погибло от перегрева!",
    TIMER_STOPPED: "",
    FREEZE_DEATH: "Яйцо погибло от холода!",
    WARMING_SUCCESS: "Яйцо нагрето!",
    WARMING_FAILED: "Не удалось нагреть яйцо.",
    EGG_ALREADY_DEAD: "Яйцо уже погибло."
};

// ========================================
// ВЫЧИСЛЯЕМЫЕ НАСТРОЙКИ
// ========================================

// Размер свайпа в пикселях
const SWIPE_THRESHOLD_PIXELS = EGG_WIDTH * (SWIPE_THRESHOLD_PERCENT / 100);

// Время инкубации в секундах
const INCUBATION_TIME_SECONDS = FAST_MODE ? 
    (INCUBATION_TIME_HOURS * 3600 * FAST_MODE_MULTIPLIER) : 
    (INCUBATION_TIME_HOURS * 3600); 